{
  "name": "budget-plan-www",
  "private": true,
  "scripts": {
    "build:blog": "python3 scripts/build_blog.py",
    "check:analytics": "node scripts/check-google-analytics.cjs",
    "generate:blog": "python3 scripts/generate_blog_post.py",
    "install:browsers": "node scripts/install-playwright.cjs",
    "test:blog": "python3 scripts/test_blog_markdown_snippets.py && python3 scripts/test_blog_automation.py",
    "test": "npm run check:analytics && npm run test:blog && playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
